// const shuffleAnswers = require('../src/app/game');

// test('Properly sets icon to the chosen category', () => {
//     expect(
//         shuffleAnswers([1, 2, 3]).toEqual([1, 2, 3] || [2, 1, 3] || [3, 2, 1] || [1, 3, 2] || [3, 1, 2] || [2, 3, 1]),
//     );
// });
